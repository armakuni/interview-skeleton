source 'https://rubygems.org'

# You do not have to use all / any of the gems below.
# They are some of the gems we use most frequently and are here to help you get set up.

gem 'grape'
gem 'puma'
gem 'rails'
gem 'sinatra'
gem 'thin'

group :development, :test do
  gem 'pry'
end

group :test do
  gem 'capybara'
  gem 'cucumber'
  gem 'rspec'
end
